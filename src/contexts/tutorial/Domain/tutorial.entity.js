export class Tutorial {
  constructor({ id = '', title = '', category = '', difficulty = '' }) {
    this.id = id
    this.title = title
    this.category = category
    this.difficulty = difficulty
  }
}
